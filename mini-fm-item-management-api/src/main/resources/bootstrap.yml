spring:
  application:
    name: microservice-course-item-management
  cloud:
    config:
      uri: http://localhost:8010
      enabled: true
  sleuth:
    sampler:
      probability: 0.25

resilience4j:
  retry:
    instances:
      item-management:
        max-attempts: 5
        wait-duration: 2s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
  ratelimiter:
    instances:
      item-management:
        limit-for-period: 5
        limit-refresh-period: 10s




